// Utility for react-day-picker locales
import { nb, da } from 'date-fns/locale';

// Export standard locales directly
// date-fns locales already handle firstDayOfWeek and formatting correctly
export const nbDayPicker = nb;
export const daDayPicker = da;
